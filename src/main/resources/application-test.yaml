spring:
  data:
    redis:
      port:
      host:
      postTtl: 1 # in days
      maxPostCountInFeed: 500
      defaultTtl: 12 # hours
      commentTtl: 24 # hours

    kafka:
      consumerConfig:
        bootstrapServersConfig: localhost:9092
        groupId: 1
        autoOffsetReset: "earliest"
        enableAutoCommit: false
        interval: 1000
        maxPollRecords: 100
        sessionTimeout: 15000
        trustedPackages: "*"
      producerConfig:
        bootstrapServersConfig: localhost:9092
        acks: "all"
        retries: 5
        idempotence: true
      topics:
        commentLikeTopic:
          name: comment_likes
          numPartitions: 5
          replicationFactor: 1
        postLikeTopic:
          name: post_likes
          numPartitions: 5
          replicationFactor: 1
        postCreatedTopic:
          name: postsTopic
          numPartitions: 5
          replicationFactor: 1